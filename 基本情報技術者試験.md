### Knowledge
#### Bits and Bytes

1 byte = 8 bits

PC works with the theory of 0 or 1.

Data are represented by sets of bits.

Alphabets are normally OK with 1 byte but Japanese alphabets need 2 bytes to represent all.

10進数 = 箱の大きさが9 = 使える数字が10 (0を含む)

n進数 = 箱の大きさがn-1 = 使える数字がn (0を含む)

##### Base 10 to Base 2
123 => 1111011

128 + 64 + 32 + 16 + 8 + 4 + 2 + 1
 0  +  1 +  1 +  1 + 1 + 0 + 1 + 1

or 

123/2 = 61 (1) = 30 (1) = 15 (0) = 7 (1) = 3 (1) = (1) 1
From counting backwards, 1111011

##### Base 2 to Base 10

1111011 => 123

 0  +  1 +  1 +  1 + 1 + 0 + 1 + 1
128 + 64 + 32 + 16 + 8 + 4 + 2 + 1

64 + 32 + 16 + 4 + 2 + 1 = 123

論理積 (AND)
論理和 (OR)
否定 (NOT)

これらを組合せばコンピューターを作ることができます。
With only AND and NOT, it will work. 

The Opposite
否定論理積 (NAND)
否定論理和 (NOR)
排他的論理和 (XOR) if the same, results 0.

###### Subtraction

Addition can be used as subtraction.
負の数を使う必要があります。
これを補数表現といいます。

123 + 876 = 999
     9の補数
123 + 877 = 1000
    10の補数

11001 = 00110 = 1の補数
00110 + 1 = 00111 = 2の補数

Find the answer of 11111 - 11011.
11111 - 11011 = 100
11111 + 00101 = 100100 = 00100

###### Multiplication and Division

論理シフト
   00001101 = 13
 0000110100 = 52
13 * 4(2^2) = 52 means shifting left 2 times.

Division means shifting right.

算術シフト：符号を考えてずらす
0 means 正 plus and 1 means 負 minus.

#### 情報理論

10111001111
このファイルを速く送るために圧縮したら、1,0,13,02,14

可逆圧縮：完全に元に戻せること
不可逆圧縮：元に戻しても不完全となること

正確に送るための技術：誤り制御
0と1を間違って送っちゃうこと
パリティチェックで防ぐ：データの誤りを検出する仕組み
偶数パリティ：1010101(1)：The last 1 means the total number of digits is odd. 

平均符号長が短くなるように符号を割り当てる技術：ハフマン符号化 (Huffman Coding)
A:3 times, B: 2 times: C: 1 time
ABABCA
The alphabet with the most appearance should be assigned with shortest number and it goes on.
0 represents A, 10 represents B, 11 represents C
Gives: 010010110














#### Database

ACID とは、トランザクションを定義する4つの重要な特性、
Atomicity（原子性）、Consistency（一貫性）、Isolation（独立性）、Durability（永続性）

正規化：DBの一貫性を保つために必要な処理
第1正規化：（カラムの）繰り返しをなくした形
第2正規化：部分関数従属しているカラムを切り出した形
第3正規化：主キー以外の列に関数従属している列を切り出した形

関数従属：あるデータが決まれば、あるデータが分かる（the whole record）
部分関数従属：same but part of the record

主キーだけで特定できない場合は、複合キーでやる

Transaction：一連の不可分な処理
コミット：トランザクション成功
ロールバック：トランザクション失敗（更新前の状態に戻す）
ロールフォワード：DB自体の失敗（現在の全ての処理を終了し、最新のバックアップ状態に戻す）

排他制御：DBロック
共有ロック：読み込めるが書き込めない
専有ロック：どっちもできない
デッドロック：two transactions wait for each other to complete the task resulting in endless lock

2相コミット：準備と確定の2回コミットを行う方法
Replication
Hot Standby, Warm Standby, Cold Standby

#### Network

LAN (Local Area Network) = Part of WAN (Wide Area Network)
スター型
バス型
リング型

Unicast - send data only to ONE device in the network
Multicast - to MULTIPLE devices
Broadcast - to ALL devices
Anycast - to ANY device

Packet are used to transfer data, consisting of header (address to be sent) and data.

⓵CSMA/CD Method (Carrier Sense Multiple Access Collision Detection)
    The data are sent using a cable and in case of failure such as data collision by multiple devices, it will be retried.
⓶Token Method
    Tokens are used to confirm whether the data should be transferred in the network.

##### Protocols

通信ルール、通信方式、物理デバイスを用いて通信を行う

###### OSI基本参照モデル

インターネットにおける通信モデルのデファクトスタンダード
上3:上位層 (HTTP, FTPなど) 下4:下位層 (TCP, IPv4/6, Ethernet・PPP)
第7層:Application - サービス
第6層:Presentation - データ形式
第5層:Session - 通信の内容
第4層:Transport - 信頼性の確保
第3層:Network - ネットワーク間通信法
第2層:Datalink - 同一ネットワーク内通信法
第1層:Physical - ケーブルの繋ぎ方

Devices Used in Each Layer
4-7: Gateway, Layer 4-7 Switch
3: Router, Layer 3 Switch
2: Bridge, Layer 2 Switch
1: NIC (Network Interface Card), LAN Cable, Repeater

OpenFlow
SDN (Software-Defined Networking)を実現するための技術
Network by hardware (Switch, Hub, Router, etc...) are different with their use and sometimes, they are inconvenient.
So, OpenFlow Controller (Middleware) are used with OpenFlow Switch to define the use of the hardware.

###### IP Addresses

MAC address - 階層性なし、IP address - あり
OSI Layer - Datalink, Network
Purpose - 同一データリンク内での端末分け、ルーティング

192.168.1.1
192.168.1 = Network Address
1 (last one) = Host Address

8 bit illustration
Global IP 192.168.1.1
xxxxxxxx xxxxxxxx xxxxxxxx xxxxxxxx
With Subnet Mask, 11111111 11111111 11111111 00000000
1 = Network part, 0 = Host part
192.168.1.1/24
meaning 24 bits for network part and 32 - 24 = 8 for host

NAT devices are used to transform public to private address and vice versa.

DHCP gives unique IP address automatically.

Ports
80, 443 - HTTP/HTTPS
20, 21 - FTP
23 - Telnet
25 - SMTP　電子メール配送 (between mail servers and from client to ms)
110 - POP　電子メール受取 (from ms to client)
123 - NTP　時刻合わせ (adjusts time automatically)

### Old questions

桁落ち：5.888888 - 5.888880 = 0.000008（有効桁数が減った）
オーバフロー：演算結果が処理できる数値の最大値を超えてしまうこと
丸め誤差：四捨五入、切り捨てによる誤差
情報落ち：98765432111111 - 1 = 98765432111110

順列：Permutation：where order matters
組み合わせ：Combination：where order does not matter

CPUの性能：MIPS（Millions Instructions Per Second）で表現
1命令に必要なクロック数の平均は、sum of (命令実行に必要クロック数 ✖ 出現率)

内部割込みと外部割込みの違い
特徴	                内部割込み	                              外部割込み
発生源	        ソフトウェア（プログラムやエラー）	    ハードウェア（外部デバイス、タイマーなど）
発生タイミング      プログラムの実行中に発生	        外部デバイスやタイマーによって発生
例	            ゼロ除算、無効なメモリアクセス	         入出力割込み、タイマー割込み
目的	        エラー処理、システムコール処理	      外部デバイスとの通信、スケジューリング
システムへの影響	プログラムのエラーハンドリング	    実行中のプロセスを中断して処理する

Cold Standby：standby while performing other tasks
Hot Standby：standby while performing the same task

稼働率
直列：a×b
並列：1-(1-a)×(1-b)

りんか：複数のモジュールから1つのロードモジュールを生成

DRAM：needs power, refresh action and used as main memory of PC
SRAM：needs power, no need refreshing and high speed. built with flip-flop style, expensive and not for large memory.
        so used as cache memory
Flash memory：non-volatile memory (不揮発性) with NAND and NOR and used in SSD

Three Operations of RDS
選択 (Selection): Filter rows based on a condition (like a WHERE clause).
射影 (Projection): Select specific columns from a table.
和 (Union): Combine the results of two tables or queries into one, eliminating duplicates.

Dumpファイル (Dump File)
説明: データベースの状態やデータをファイルに保存したもの。主にバックアップや移行に使用されます。
用途: データベースを他のシステムに移行したり、バックアップとして保存したりします。

チェックポイントファイル (Checkpoint File)
説明: データベースの進行状況や状態を定期的に保存するためのファイル。障害発生時にデータを回復するために使用されます。
用途: データベースの障害復旧や、途中の状態を保存するために使用されます。

バックアップファイル (Backup File)
説明: データベースやシステムのコピーを保存したファイル。データの損失を防ぐために使用されます。
用途: システムやデータベースを復元するために使用されます。

ログファイル (Log File)
説明: システムやアプリケーションの操作履歴を記録するファイル。エラーのトラブルシューティングや監査に使用されます。
用途: システムの動作履歴やエラーメッセージを記録し、後で調査するために使用されます。

DHCP (Dynamic Host Configuration Protocol)
動的ホスト構成プロトコル。ネットワークに接続された機器（コンピュータやスマートフォンなど）に、自動的にIPアドレスやネットワーク設定を割り当てる仕組みです。

NAT (Network Address Translation)
ネットワークアドレス変換。内部ネットワークのプライベートIPアドレスを、外部ネットワーク（インターネット）に接続するために使うパブリックIPアドレスに変換する技術です。

NAPT (Network Address and Port Translation)
ネットワークアドレスおよびポート番号変換。NATの一種で、IPアドレスだけでなく、ポート番号も変換することで、複数の機器が1つのパブリックIPアドレスを共有してインターネットにアクセスできるようにする技術です。

Gateway (ゲートウェイ)
定義: 異なるネットワーク間で通信を仲介するデバイスです。ネットワークプロトコルや形式が異なる場合でもデータを変換して送信します。
OSI層: 第7層（アプリケーション層）まで動作します。主に異なるプロトコルを扱うため、アプリケーション層から通信を行います。

Bridge (ブリッジ)
定義: 同一のネットワーク内で、データを転送し、ネットワークを分割して効率的にデータを流します。データの転送先を決定する際、MACアドレスを基に判断します。
OSI層: 第2層（データリンク層）。MACアドレスを使ってデータを転送します。

Repeater (リピーター)
定義: 信号を増幅して再送信し、信号の距離を延ばすデバイスです。長距離通信時に信号が弱くならないようにします。
OSI層: 第1層（物理層）。信号を増幅して転送します。

Router (ルーター)
定義: 異なるネットワーク間でデータを転送するデバイスです。IPアドレスを基に、最適な経路を選んでデータを送信します。
OSI層: 第3層（ネットワーク層）。IPアドレスを使ってデータを転送します。

SEOポイズニング：showing the abusive one at the top of the search results

AES：共通鍵暗号式（used same key for encryption and decryption）
RSA：公開鍵暗号式（public key for encryption or validation of sign, private key for decryption or signing）

False Rejection Rate： the possibility of rejecting the true one
False Accept Rate： the possibility of accepting the false one

